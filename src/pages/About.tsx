"use client";

import styles from "@/scss/About.module.scss";

const About = () => {
    return (
        <div className={styles.s_about}>
            <div className={styles.about_inner}>
                <h1>Discover the Journey with Horizon Tales.</h1>
            </div>

        </div>
    );
};

export default About;
